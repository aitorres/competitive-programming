/**
 * Competitive programming C++ template.
 * This template will be improved over time.
 *
 * Please, always check this file because it includes the latest
 * template I use, which might not be the case on solved problem
 * files if they were added to the repository before a given template 
 * modification.
 *
 * @author: Andrés Ignacio Torres
 *          (@aitorres on Github)
 *          (@andresitorresm on Twitter)
 */

#include <bits/stdc++.h>

#define FOR(i, j, k, in) for (int i=j ; i<k ; i+=in)
#define RFOR(i, j, k, in) for (int i=j ; i>=k ; i-=in)
#define RI(x) scanf("%i", &x)
#define RII(x, y) scanf("%i %i", &x, &y)
#define RIII(x, y, z) scanf("%i %i %i", &x, &y, &z)
#define PI(x) printf("%i\n", x)
#define PII(x, y) printf("%i %i\n", x, y)
#define PIII(x, y, z) printf("%i %i %i\n", x, y, z)

using namespace std;

int main(int argc, char const *argv[])
{
	// Code goes here
	return 0;
}
